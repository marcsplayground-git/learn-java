package com.personalplayground.bankooplearning.demo;

public class BankAppStep1 {
    public static void main(String[] args) {

        /* Done

        // Create instances (objects) from the BankAccount class
        BankAccount checking = new BankAccount("CHK-001", "Alice", 1000.0);
        BankAccount savings = new BankAccount("SVG-001", "Bob", 2000.0);

        // Use methods instead of manipulating arrays directly
        checking.deposit(200);
        savings.withdraw(300.0);

        System.out.println("Checking (" + checking.getId() + ", " + checking.getOwnerName()
                + ") balance: " + checking.getBalance());

        System.out.println("Savings (" + savings.getId() + ", " + savings.getOwnerName()
                + ") balance: " + savings.getBalance());

         */
    }
}
